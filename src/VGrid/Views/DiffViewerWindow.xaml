<Window x:Class="VGrid.Views.DiffViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:VGrid.Converters"
        Title="{Binding DiffTitle, Mode=OneWay}" Height="700" Width="1200"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <converters:DiffStatusToColorConverter x:Key="DiffStatusToColorConverter"/>

        <!-- Excel-like DataGrid Style -->
        <Style x:Key="ExcelDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#D0D0D0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#D0D0D0"/>
            <Setter Property="RowHeaderWidth" Value="40"/>
            <Setter Property="ColumnHeaderHeight" Value="25"/>
            <Setter Property="BorderBrush" Value="#BEBEBE"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Excel-like Column Header Style -->
        <Style x:Key="ExcelColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#BEBEBE"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- Excel-like Row Header Style -->
        <Style x:Key="ExcelRowHeaderStyle" TargetType="DataGridRowHeader">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#BEBEBE"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Width" Value="40"/>
        </Style>

        <!-- Excel-like Cell Style -->
        <Style x:Key="ExcelCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D4E7F7"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderBrush" Value="#4A90E2"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- File List -->
            <Border Grid.Column="0" BorderBrush="#CCC" BorderThickness="0,0,1,0" Background="White">
                <ListBox ItemsSource="{Binding ChangedFiles}"
                         SelectedItem="{Binding SelectedFile}"
                         BorderThickness="0"
                         Background="White"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=., Mode=OneWay}"
                                       ToolTip="{Binding Path=., Mode=OneWay}"
                                       Padding="5,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#E0E0E0"/>

            <!-- Diff Display Area -->
            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                    <!-- Left DataGrid (Old Version) -->
                    <Border Grid.Column="0" BorderBrush="#CCC" BorderThickness="1">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="{Binding LeftHeader, Mode=OneWay}"
                                       Background="#F0F0F0" Padding="5" FontWeight="Bold" TextAlignment="Center"/>
                            <DataGrid x:Name="LeftDataGrid"
                                      ItemsSource="{Binding LeftRows}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="All"
                                      CanUserResizeRows="False"
                                      CanUserReorderColumns="False"
                                      SelectionMode="Single"
                                      SelectionUnit="Cell"
                                      Style="{StaticResource ExcelDataGridStyle}"
                                      ColumnHeaderStyle="{StaticResource ExcelColumnHeaderStyle}"
                                      RowHeaderStyle="{StaticResource ExcelRowHeaderStyle}"
                                      CellStyle="{StaticResource ExcelCellStyle}"/>
                        </DockPanel>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#E0E0E0"/>

                    <!-- Right DataGrid (New Version) -->
                    <Border Grid.Column="2" BorderBrush="#CCC" BorderThickness="1">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="{Binding RightHeader, Mode=OneWay}"
                                       Background="#F0F0F0" Padding="5" FontWeight="Bold" TextAlignment="Center"/>
                            <DataGrid x:Name="RightDataGrid"
                                      ItemsSource="{Binding RightRows}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="All"
                                      CanUserResizeRows="False"
                                      CanUserReorderColumns="False"
                                      SelectionMode="Single"
                                      SelectionUnit="Cell"
                                      Style="{StaticResource ExcelDataGridStyle}"
                                      ColumnHeaderStyle="{StaticResource ExcelColumnHeaderStyle}"
                                      RowHeaderStyle="{StaticResource ExcelRowHeaderStyle}"
                                      CellStyle="{StaticResource ExcelCellStyle}"/>
                        </DockPanel>
                    </Border>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
